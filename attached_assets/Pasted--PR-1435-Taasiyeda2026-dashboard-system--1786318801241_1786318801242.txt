ריפליט, יש לטפל ב-PR #1435 בריפו:

`Taasiyeda2026/dashboard_system`

המטרה היא לזהות ולתקן את הכשל המדויק ב-GitHub Actions, ללא ניחושים וללא שינויים רחבים.

1. יש לעבוד על הענף:
   `codex/finalize-permissions-and-api-test-selection`

2. לפני כל שינוי:

   * ודא שה-repository מחובר ל-GitHub.
   * ודא שקיים Git remote תקין.
   * משוך את הגרסה העדכנית של הענף.
   * ודא שה-HEAD המקומי תואם ל-HEAD הנוכחי של PR #1435.

3. פתח את GitHub Actions של PR #1435 ובדוק את הכשל המדויק ב-job:
   `focused syntax, tests and build`

   ובשלב:
   `Run changed-file plan`

   שמריץ:
   `npm run ci:quick`

4. אין לנחש מה התקלה.
   יש לזהות מתוך ה-log:

   * איזה test נכשל.
   * מה ה-assertion או השגיאה המדויקת.
   * באיזה קובץ.
   * מה הסיבה לכשל.

5. חשוב:
   הציפייה של `USER_PUBLIC_COLUMNS` כבר כוללת את `default_view` במיקום הנכון בגרסת ה-PR הנוכחית.

   לכן אין לשנות שוב את הנושא הזה אלא אם ה-log הנוכחי מוכיח במפורש שהוא עדיין הגורם לכשל.

6. יש לבצע תיקון מינימלי בלבד:

   * אם מדובר ב-test לא מעודכן - לתקן רק את ה-test הנדרש.
   * אם ה-test חושף תקלה אמיתית בקוד - לתקן רק את החלק הרלוונטי בקוד.
   * אין לבצע refactor רחב.
   * אין לשנות UI.
   * אין לשנות לוגיקה עסקית שאינה קשורה לכשל.
   * אין לגעת בקבצים שאינם נדרשים.

7. אין לבטל או להרחיב מחדש את מנגנון ה-CI החדש.
   יש לשמור על ההתנהגות שבה:

   * Full Regression אינו רץ על כל PR.
   * `ci:quick` בוחר בדיקות לפי הקבצים שהשתנו.
   * שינויי permissions בלבד אינם אמורים לבחור בטעות בדיקות Activities בגלל מחרוזות כלליות כמו `SUPABASE_ROLE_ROUTES`.

8. לאחר התיקון יש להריץ בסדר הבא:

   * קודם את ה-test המדויק שנכשל.
   * לאחר מכן:
     `node --test tests/ci-check-plan.test.mjs`
   * לאחר מכן:
     `npm run ci:quick`
   * ולבסוף:
     `git diff --check`

9. אם הכל תקין:

   * בצע commit ברור וממוקד.
   * בצע push לאותו branch של PR #1435.
   * ודא שה-PR עודכן בפועל ב-GitHub.
   * ודא שה-GitHub Actions החדש התחיל.
   * בדוק שה-checks של PR #1435 ירוקים.

10. אין לבצע merge ל-main בשלב הזה.

בסיום החזר לי בלבד:

1. מה בדיוק נכשל.
2. מה היה שורש התקלה.
3. איזה קובץ או קבצים תוקנו.
4. מה בדיוק שונה.
5. SHA של ה-commit החדש.
6. האם ה-push הצליח.
7. האם PR #1435 ירוק.
8. אם עדיין יש כשל - את שם ה-test והשגיאה המדויקת.

בדיקות קבלה:

* PR #1435 ירוק.
* אין Full Regression מיותר.
* אין בחירה שגויה של Activities עבור שינוי permissions בלבד.
* אין שינויים לא קשורים.
* אין שינויי UI או לוגיקה עסקית שאינם חלק מהתקלה.
