name: Export scheduling-route 20260916

on:
  push:
    branches:
      - codex/attendance-route-autofill-20260916

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: scheduling-route-source-20260916
          path: supabase/functions/scheduling-route/index.ts
          if-no-files-found: error
