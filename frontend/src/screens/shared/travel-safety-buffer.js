/**
 * Single source of truth for the operational travel safety buffer.
 * Used only in feasibility checks (gaps vs raw travel), never in scoring or route cache.
 */
export const TRAVEL_SAFETY_BUFFER_MINUTES = 15;
