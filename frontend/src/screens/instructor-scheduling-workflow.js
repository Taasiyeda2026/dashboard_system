import { supabase } from '../supabase-client.js';
import { escapeHtml } from './shared/html.js';
import { rankInstructors, deriveEducationLevel, adjacentActivities } from './instructor-matching-engine.js';
import { showToast } from './shared/toast.js';
const txt=value=>String(value ?? '').trim();
const group=(rows,key='emp_id')=>rows.reduce((out,row)=>((out[txt(row[key])] ||= []).push(row),out),{});
const meetings=activity=>Array.from({length:35},(_,i)=>activity[`date_${i+1}`]).filter(Boolean).map(date=>({date,start_time:activity.start_time,end_time:activity.end_time}));
async function route(origin,destination){if(!txt(origin)||!txt(destination))return null;try{const {data,error}=await supabase.functions.invoke('scheduling-route',{body:{origin,destination}});if(error||!data?.calculated)return null;return {distance_km:Number(data.distance_km),duration_minutes:Number(data.duration_minutes)};}catch{return null;}}
async function candidateTravel(instructor,activity,assigned){const result={home:await route(instructor.address,activity.school_address || activity.school),transitions:{}};for(const meeting of meetings(activity)){const {previous,next}=adjacentActivities(assigned,meeting);result.transitions[meeting.date]={previous:previous?await route(previous.school_address || previous.school,activity.school_address || activity.school):null,next:next?await route(activity.school_address || activity.school,next.school_address || next.school):null};}return result;}
function neighborLine(label,item,leg){if(!item)return `${label}: אין`;const travel=leg?.duration_minutes!=null?` · ${leg.duration_minutes} דקות, ${Math.round(leg.distance_km)} ק״מ`:' · זמן מעבר טרם חושב';return `${label}: ${item.activity_name || 'פעילות'} (${item.start_time}–${item.end_time})${travel}`;}
function card(candidate,topId){const i=candidate.instructor;return `<article class="ds-card" style="padding:14px;display:grid;gap:7px"><div style="display:flex;justify-content:space-between"><strong>${escapeHtml(i.full_name || i.emp_id)}</strong>${candidate.score==null?'':`<span class="ds-badge">ציון ${candidate.score}</span>`}</div><span class="ds-muted">מזהה: ${escapeHtml(i.emp_id)} · שפות: ${escapeHtml((candidate.profile?.instruction_languages || ['he']).map(x=>x==='he'?'עברית':'ערבית').join(', '))}</span><p style="margin:0">${escapeHtml(candidate.eligible?candidate.explanation:candidate.failures.join(' · '))}</p>${candidate.warnings.length?`<p style="margin:0;color:#9a6700">⚠ ${escapeHtml(candidate.warnings.join(' · '))}</p>`:''}${candidate.eligible?`<button type="button" class="ds-btn ds-btn--primary" data-select-scheduling-candidate="${escapeHtml(i.emp_id)}" data-top="${txt(i.emp_id)===topId?'yes':'no'}">בחירה</button>`:''}</article>`;}
export function bindInstructorScheduling(root,{ui,state,onAssigned,onCalculated}={}){const button=root?.querySelector('[data-find-instructor]');if(!button||button.dataset.bound)return;button.dataset.bound='yes';button.addEventListener('click',async()=>{if(!['admin','operation_manager'].includes(txt(state?.user?.role)))return showToast('אין הרשאה לשמור שיבוץ','error');const form=button.closest('[data-drawer-form]'),activity=JSON.parse(form?.dataset.exportRow || '{}');activity.instruction_language=form.querySelector('[name="instruction_language"]')?.value || activity.instruction_language || 'he';activity.required_instructor_gender=form.querySelector('[name="required_instructor_gender"]')?.value || activity.required_instructor_gender || 'any';activity.education_level=form.querySelector('[name="education_level"]')?.value || activity.education_level || deriveEducationLevel(activity.grade);button.disabled=true;button.textContent='מחשב התאמות ונסיעות…';try{const [contacts,profiles,rules,exceptions,activities]=await Promise.all([supabase.from('contacts_instructors').select('*'),supabase.from('instructor_scheduling_profiles').select('*'),supabase.from('instructor_availability_rules').select('*'),supabase.from('instructor_availability_exceptions').select('*'),supabase.from('activities').select('*').neq('row_id',txt(activity.row_id || activity.RowID))]);const error=[contacts,profiles,rules,exceptions,activities].find(x=>x.error)?.error;if(error)throw error;const profileMap=Object.fromEntries(profiles.data.map(p=>[txt(p.emp_id),p])),assignments={};for(const a of activities.data){for(const id of [txt(a.emp_id),txt(a.emp_id_2)].filter(Boolean)){for(const m of meetings(a))(assignments[id] ||= []).push({...m,activity_name:a.activity_name,school:a.school,school_address:a.school_address,authority:a.authority});}}const travel={},weeklyLoads={};await Promise.all(contacts.data.map(async instructor=>{const id=txt(instructor.emp_id);weeklyLoads[id]=(assignments[id] || []).length;travel[id]=await candidateTravel(instructor,activity,assignments[id] || []);}));const averageWeeklyLoad=contacts.data.length?Object.values(weeklyLoads).reduce((a,b)=>a+b,0)/contacts.data.length:0;const results=rankInstructors({instructors:contacts.data,profiles:profileMap,rules:group(rules.data),exceptions:group(exceptions.data),assignments,travel,weeklyLoads,averageWeeklyLoad,activity});[...results.recommended,...results.exceptions,...results.rejected].forEach(c=>{c.profile=profileMap[txt(c.instructor.emp_id)] || {};});const eligible=[...results.recommended,...results.exceptions],top=eligible[0];onCalculated?.({ activityId:txt(activity.row_id||activity.RowID), ready:eligible.length>0, candidateCount:eligible.length, topName:top?.instructor?.full_name || '', reason:eligible.length?'':'לא נמצאו מדריכים שעברו את תנאי הסף' });const section=(title,rows)=>`<section style="display:grid;gap:9px"><h3>${title} <span class="ds-badge">${rows.length}</span></h3>${rows.map(c=>card(c,txt(top?.instructor.emp_id))).join('') || '<p class="ds-muted">אין מועמדים בקבוצה זו.</p>'}</section>`;ui.openDrawer({title:'איתור ושיבוץ מדריך',content:`<div dir="rtl" style="display:grid;gap:16px"><header><h2>${escapeHtml(activity.activity_name || 'פעילות')}</h2><p>${escapeHtml(activity.school || '—')} · ${escapeHtml(activity.authority || '—')} · ${escapeHtml(activity.education_level || 'שכבה לא הוגדרה')} · ${activity.instruction_language==='ar'?'ערבית':'עברית'} · ${escapeHtml(activity.start_time || '')}–${escapeHtml(activity.end_time || '')}</p><strong>${eligible.length?'מוכן לשיבוץ':'אין מועמדים מתאימים'}</strong></header>${section('מומלצים',results.recommended)}${section('מתאימים עם חריגה',results.exceptions)}${section('לא מתאימים',results.rejected)}</div>`,onOpen:drawer=>{drawer.querySelectorAll('[data-select-scheduling-candidate]').forEach(select=>select.addEventListener('click',()=>{const candidate=eligible.find(c=>txt(c.instructor.emp_id)===select.dataset.selectSchedulingCandidate),exception=candidate.warnings.length>0,override=select.dataset.top!=='yes';const schedule=candidate.schedule.map(s=>`<li><strong>${escapeHtml(s.date)}</strong><br>${escapeHtml(neighborLine('פעילות קודמת',s.previous,s.previous_travel))}<br>${escapeHtml(neighborLine('פעילות הבאה',s.next,s.next_travel))}</li>`).join('');ui.openModal({title:'אישור שיבוץ',content:`<div dir="rtl"><h3>${escapeHtml(candidate.instructor.full_name)}</h3><p>כל המפגשים בשעות ${escapeHtml(activity.start_time||'')}–${escapeHtml(activity.end_time||'')}:</p><ul>${schedule}</ul>${candidate.warnings.length?`<p>חריגות: ${escapeHtml(candidate.warnings.join(' · '))}</p>`:''}<label>${override||exception?'סיבה (חובה)':'הערה'}<textarea class="ds-input" data-assignment-reason></textarea></label>${exception?'<label><input type="checkbox" data-approve-exception> אני מאשר/ת את החריגה</label>':''}<p data-assignment-error></p></div>`,actions:'<button class="ds-btn ds-btn--primary" data-confirm-assignment>אישור ושיבוץ</button><button class="ds-btn" data-ui-close-modal>ביטול</button>'});document.querySelector('[data-confirm-assignment]')?.addEventListener('click',async()=>{const modal=document.querySelector('.ds-modal__content'),reason=txt(modal.querySelector('[data-assignment-reason]')?.value),err=modal.querySelector('[data-assignment-error]');if((override||exception)&&!reason){err.textContent='חובה להזין סיבה.';return;}if(exception&&!modal.querySelector('[data-approve-exception]')?.checked){err.textContent='חובה לאשר את החריגה.';return;}try{const decision=exception?'exception_approved':override?'overridden':'approved';const {error}=await supabase.rpc('assign_activity_instructor',{p_activity_id:txt(activity.row_id||activity.RowID),p_emp_id:Number(candidate.instructor.emp_id),p_instructor_name:candidate.instructor.full_name,p_top_emp_id:top?Number(top.instructor.emp_id):null,p_selected_score:candidate.score,p_top_score:top?.score??null,p_decision_type:decision,p_reason:reason||null});if(error)throw error;ui.closeModal();ui.closeDrawer();showToast('המדריך שובץ בהצלחה','success');onAssigned?.();}catch(e){err.textContent=`השמירה נכשלה: ${e.message}`;}});}));}});}catch(e){showToast(`חישוב ההתאמות נכשל: ${e.message}`,'error');}finally{button.disabled=false;button.textContent='איתור ושיבוץ מדריך';}});}
